--- a/src/OVAL/probes/unix/linux/dpkginfo-helper.cxx
+++ b/src/OVAL/probes/unix/linux/dpkginfo-helper.cxx
@@ -22,7 +22,6 @@
 using namespace std;
 
 static int _init_done = 0;
-static pkgCacheFile *cgCache = NULL;
 
 static int opencache (void) {
         if (pkgInitConfig (*_config) == false) return 0;
@@ -46,24 +45,17 @@ static int opencache (void) {
 
         if (pkgInitSystem (*_config, _system) == false) return 0;
 
-        if (_error->PendingError () == true) {
-                _error->DumpErrors ();
-                return 0;
-        }
-
         return 1;
 }
 
 struct dpkginfo_reply_t * dpkginfo_get_by_name(const char *name, int *err)
 {
-        pkgCache &cache = *cgCache->GetPkgCache();
-        pkgRecords Recs (cache);
+        pkgCacheFile CacheFile;
+        pkgCache *cache = CacheFile.GetPkgCache();
         struct dpkginfo_reply_t *reply = NULL;
 
-        if (!cgCache->ReadOnlyOpen(NULL)) return 0;
-
         // Locate the package
-        pkgCache::PkgIterator Pkg = cache.FindPkg(name);
+        pkgCache::PkgIterator Pkg = cache->FindPkg(name);
         if (Pkg.end() == true) {
                 /* not found, clear error flag */
                 if (err) *err = 0;
@@ -77,7 +69,6 @@ struct dpkginfo_reply_t * dpkginfo_get_by_name(const char *name, int *err)
                 if (err) *err = 0;
                 return NULL;
         }
-        pkgRecords::Parser &P = Recs.Lookup(V1.FileList());
 
         /* split epoch, version and release */
         string evr = V1.VerStr();
@@ -137,10 +128,7 @@ void dpkginfo_free_reply(struct dpkginfo_reply_t *reply)
 int dpkginfo_init()
 {
         if (_init_done == 0) {
-                cgCache = new pkgCacheFile;
                 if (opencache() != 1) {
-                        delete cgCache;
-                        cgCache = NULL;
                         return -1;
                 }
                 _init_done = 1;
@@ -151,10 +139,6 @@ int dpkginfo_init()
 
 int dpkginfo_fini()
 {
-        if (cgCache != NULL) {
-                cgCache->Close();
-        }
-
         return 0;
 }
 
